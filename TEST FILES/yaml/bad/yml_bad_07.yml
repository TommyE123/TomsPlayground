########################
########################
## Configuration File ##
########################
########################

# Rule: document-start - Missing document start marker
database:
  host: localhost
  port: 5432
  user: admin
  password: admin # Rule: trailing-spaces - Trailing spaces are disallowed
server:
  host: 0.0.0.0
  port: 8080
  ssl:
    enabled: true
    certificate: /path/to/cert.pem
    key: /path/to/key.pem
logging: 
   level: DEBUG # Rule: indentation - Inconsistent indentation
  file: /var/log/webapp.log
features:
  enable_feature_x: true
  enable_feature_y: false

